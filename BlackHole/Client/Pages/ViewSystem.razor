@page "/system"
@inject IStringLocalizer<Texts> S

<BlackHoleTitle Title="@S["System"]" />

@if (Config != null)
{
    @foreach (var c in Config)
    {
        <p><strong>@(S.Get(c.Key))</strong>: @(c.Value)</p>
    }
}
